in main.py to generate qr

@app.get("/generate/mail")
def generate2(email: str):
    img = generate_qr_mail(email)
    return create_image_response(img)

@app.get("/generate/wifi")
def generate3(security: str, ssid: str, password: str=""):
    img = generate_qr_wifi(security,ssid,password)
    return create_image_response(img)
